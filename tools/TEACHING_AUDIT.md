# 全站教學與來源修訂

日期：2026-09-05。審查基準：`83681b2`，開始時工作目錄乾淨。
範圍：首頁、3 頁課前準備、11 頁正課、6 頁 Python 附錄，以及正文、自測、詞彙卡、現有題庫。
保留三區與授課章序；修訂生成來源後重建 HTML。發布方式為推送 main 分支，由 GitHub Pages 建置。

## 修訂決策

| 項目 | 已採用的修訂 |
|---|---|
| 書目與定位 | 每頁導讀介紹完整書名；章節旁以中文角色／主題與章節呈現，可跳到同頁書目。AI-Stats 僅作內部鍵；修正「書籍 Preface」、QDA/NB、CV 洩漏、PCA 尺度化來源與「章開頭」標記。來源呈現集中於 `sources.py`。 |
| 閱讀導航 | REF 改「重點速查與來源」、00C 改「課前準備 C」，移除讀者導覽中的實作 class 名稱。手機浮動章節列會遮住正文，改用既有頁內目錄與頁尾導覽。 |
| 課前學習 | 00A 講預測、嘗試、觀察與修正；00C 用課程 Auto.csv 展示提示與執行證據。刪合重複責任清單，統計／CV 案例改延伸。 |
| 起步路徑 | 00B 指定課程 notebook 與 Auto.csv，補下載、Drive 放置、掛載、路徑、特殊值及第一張圖的步驟；取消三分鐘保證，區分儲存筆記本與回收執行階段。 |
| Python 銜接 | P2 先簡單條件、迴圈、函式，再讀建模應用。補實跑前置儲存格、增加迴圈次數時同步加長陣列的指示。Ch2 重複 zip／seed 改連附錄。 |
| 分類與迴歸 | 自測統一以訓練反應變數判斷，明說分類可輸出機率；消除同頁相反判準。 |
| 偏差與高維度 | 偏差不再宣稱與 n 無關；訓練插值不保證泛化差或相同。Ch6 圖旁說明對齊既有純雜訊資料、p=1–19、400 次及平均／中位數定義。 |
| 推論 | F 檢定仍有第一型錯誤；先 F 不代表個別 t 已做多重比較校正。 |
| 分類方法 | Gaussian NB 一般對應對角 QDA，跨類別共用變異數時才對應對角 LDA；LDA 與 logistic 的函數形式相同不保證估計相同。區分多數類基準正確率與錯誤率。 |
| 評估 | 區分 LOOCV、k-fold、ShuffleSplit；固定切法不等於没有不確定性。不能以兩模型各自 SD 直接判定差異；檢查同切分配對結果。Pipeline 不保證管線外沒有洩漏。 |
| 非監督式 | 無現成群標籤不等於所有留出評估均無效。保留 loading 圖，改考變數貢獻；移除不適用的角度相關判讀與「不相關等於獨立」。釐清數學 V 與 lab 所存 Vᵀ。 |
| 局部迴歸／RF | span=1 仍依距離加權，未必是 OLS；RF 實測相關標為描述性指標，不當理論變異分解參數，也不保證隨 m 單調變化。 |
| SVM／深度學習 | margin 上 hinge=0 的點仍可能是支持向量；未縮放 logistic loss 改稱凸代理損失。IMDB 保留有競爭力的線性基準，不宣稱深度方法必勝。 |
| 附錄界線 | 遺漏指示變數不能自動消除選擇偏差；reshape 可能需要複製；盒鬚圖不是唯一能標出離群值的圖。 |

同類說法同步檢查正文、Q&A、自測回饋、圖旁文字、詞彙卡和題庫，沒有更改逐字引用的 lab 程式與保存輸出，也沒有重跑烘焙資料產生器。

## 期中考環境補充

使用者確認期中考使用電腦教室電腦。首頁、環境安裝頁與 README 強調本機練習版本建議對齊教室，並連到現行課程的 `packages.txt`。2026-09-05 以 HTTP GET 核對：Python 3.9.13、statsmodels 0.13.2、matplotlib 3.5.2；這與本站既有圖表生成環境不同。保留圖表生成版本作歷史來源，考前安裝不再使用它代替教室清單。清單是版本對照表，不是可直接 `pip install -r` 的 requirements。

## 確認無誤並保留

- PCA 幾何 → 最佳低秩近似 → 矩陣補全是不同角度的遞進，不當成冗餘刪除。
- CV 選模型、bootstrap 估不確定性的分工；獨立測試集／nested CV；Portfolio 配對重抽。
- Ch6 one-SE 例子：53,800 + 6,000 = 59,800，在列出的候選中選 4 個變數。
- SVM 的課本 budget C 與套件 penalty C 區分。
- 深度學習的補充定位，以及課本數字與官方 lab 數字的區分。
- 同機率調閾值的混淆矩陣、P1 別名、P3 廣播、P5 Anscombe 與同資料截斷軸比較。

## 驗證

記錄保存在 `verification/teaching-20260905/`。

- 全站結構、來源完整性、同頁／跨頁錨點：20 頁，0 失敗、0 警告。
- 既有視覺主張與數值不變量檢查：通過。
- 外部連結：原 88 個檢查 0 失敗；新增官方版本清單已以 GET 核對。2 個 Colab 入口對 HEAD 回 405，以 GET 補驗均為 HTTP 200。
- 全站桌面／手機互動與 CDN 失效：通過。
- 手機來源跳轉與首頁：通過。
- 保留既有 FRAMES 數值；逐字 lab 來源檢查通過。差異空白檢查通過。

邊界：Colab 入口與資料取得步驟已核對，未登入使用者帳號啟動雲端 notebook；本輪不是套件版本升級或所有統計結果的全面重算。
