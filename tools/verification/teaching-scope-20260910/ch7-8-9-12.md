# Ch7、8、9、12 教學範圍清理

本次採用已確認界線：講義直接教的完整公式與必要推導保留；講義列名方法的現有必要內容維持收合；移除非講義原例的自訂數值算例、題外獨立方法與工程重現報告。老師同章原Lab只保留直接教方法的必要原碼片段/必要短結果，不重新跑Lab。

實作入口：四個enrich母檔與reading_flow_ch7_8_9_12.py，清理規則位於teaching_scope_ch7_8_9_12.py。先依原內容分組，再按精確主題/原cell保留片段，最後生成。原始課程PDF/notebook與FRAMES均未修改。

## 逐章依據與變更

|章|保留的講義核心|移除|
|---|---|---|
|7|PDF5–7多項式/CI；8–10切段/基底/交互作用；11–23樣條/自然邊界；24–27及47平滑矩陣/LOOCV；28–33局部迴歸；34–44及48GAM/分類；46及49–54自然基底、薄板、B-spline、效果圖|自訂logit(-2,.5)、三點WLS、樣條/GAM/PDP手算、平滑矩陣diag例；完整summary/array/approx_lam實作；網站MSE排行榜與帶寬/GCV重現報告。保留多項式曲線，去除下方自造CV勝負子圖；保留平滑程度互動，移除GCV勝出曲線/控制。|
|8|PDF11–18CART/剪枝；24–25impurity；31–39投票/bagging/RF；43–51及68–69提升/重要度；52–61XGBoost/LightGBM/CatBoost/GOSS/MVS/DART；64–67樹家族/stacking/Extra-trees；74–79BART|自訂類別A/B/C、RSS10→18、G/H三點、Ada e=1/4、MVS/DART/GOSS/有序統計/BART數字例；625秒等速度表、全搜尋跑分、Boston模型MSE榜。移除跨Boston/新模擬的RF勝負實驗，保留RF數學與原Lab必要程式；保留講義impurity重要度，移除額外permutation實驗。|
|9|PDF5–18硬軟SVM，使用者指定完整primal-dual/KKT/截距；19–29kernel/OVO；30–34hinge/求解器/核近似/SVR/novelty；38–40perceptron與核。現有SVR/單類別/RFF必要推導收合保留|自訂兩點dual數字解、3-4-5距離、Gram兩點例、小ROC數字表、SVR數字例；完整support arrays/人工資料setup/完整混淆矩陣/跑分表。移除核Ridge與Nyström獨立延伸。RBF幾何互動保留，下方自造測試錯誤比較子圖移除。|
|12|PDF7–28PCA/SVD/PVE；29–38補值；39–53及99SNE/t-SNE完整公式；59–80K-means/linkage/Ward；81–85DBSCAN/HDBSCAN；92混合資料；96列名GMM/譜/變分/張量維持收合；98白化；101–105Mean shift與應用|人工PCA矩陣/奇異值/白化/2×2補值/三點t-SNE/GMM/HDBSCAN/k-prototype數字例；K-means++、獨立MST教案、Gower/PAM/CH/PHATE。保留HDBSCAN內必要MST說明。移除完整arrays、補值相關重現、verbose/UMAP分類勝負輸出；保留PVE原Lab四值與圖的必要狀態。|

## 原Lab必要片段

以下皆取原cell全文或連續片段，沒有改写原碼、改seed/初始化、改模型參數，沒有新造輸出。原程式中的random_state等必要原參數仍逐字保留；移除的是對外版本/重現宣示及不必要setup/完整結果。

### beyond_linearity

|保留示範|原Lab cell|輸出|
|---|---|---|
|擬合多項式|14|不內嵌完整輸出|
|比較多項式次數|26|不內嵌完整輸出|
|多項式邏輯斯迴歸|33|不內嵌完整輸出|
|切段與虛擬變數|39|不內嵌完整輸出|
|擬合立方樣條|46|不內嵌完整輸出|
|擬合自然樣條|56|不內嵌完整輸出|
|局部迴歸的跨距|125|不內嵌完整輸出|
|擬合加法模型|82|不內嵌完整輸出|

移除ID：`dx-bs`, `dx-bs0`, `dx-gam`, `dx-lam`, `dx-low`, `dx-ns`, `dx-poly`, `dx-step`, `dx-sum`, `qDegFeedback`, `qDegOptions`, `w08-detail-basis-lab`, `w08-detail-smooth-lab`, `w08gamGcv`, `w08gamR2`, `w08lamGcv`, `w08lamPickBtn`, `w08lamR2`, `w08natWl`, `w08natWr`, `w08polyCv`, `w08polyMse`, `w08polyTrain`, `w08polyWl`, `w08polyWm`, `w08polyWr`

### tree_based_methods

|保留示範|原Lab cell|輸出|
|---|---|---|
|擬合迴歸樹|54|不內嵌完整輸出|
|以交叉驗證選剪枝參數|57|不內嵌完整輸出|
|擬合分類樹|17|不內嵌完整輸出|
|組合多個分類器投票|238|不內嵌完整輸出|
|Bagging|64|不內嵌完整輸出|
|隨機森林與變數重要度|70|不內嵌完整輸出|
|讀取分裂重要度|72|不內嵌完整輸出|
|梯度提升|76|不內嵌完整輸出|
|XGBoost 的分類目標|124|不內嵌完整輸出|
|Stacking 合併器|240|不內嵌完整輸出|
|BART 模型|86|不內嵌完整輸出|

移除ID：`dx-bag`, `dx-bart`, `dx-ccp`, `dx-cv`, `dx-early`, `dx-ent`, `dx-gbr`, `dx-high`, `dx-imp`, `dx-reg`, `dx-stk`, `dx-vote`, `dx-xgb`, `qVoteFeedback`, `qVoteOptions`, `w09-detail-prologue-lab`, `w09rfChart`, `w09rfR0`, `w09rfR1`, `w09rfR2`, `w09rfR3`, `w09rfSel`, `w09rfSingle`, `w09rfStatus`, `w09vimpMse`, `w09vimpSel`, `w09vimpShare`, `w09vimpTop1`, `w09vimpTop2`

### support_vector_machines

|保留示範|原Lab cell|輸出|
|---|---|---|
|線性支持向量分類器|16|不內嵌完整輸出|
|辨認支持向量|22,23|不內嵌完整輸出|
|交叉驗證選 C|31|不內嵌完整輸出|
|以 hinge loss 擬合線性分類器|95|不內嵌完整輸出|
|特徵映射與自訂核|51|不內嵌完整輸出|
|用自訂核擬合|53|不內嵌完整輸出|
|RBF 核分類器|61|不內嵌完整輸出|
|同時選 C 與核參數|67|不內嵌完整輸出|
|用隨機 Fourier 特徵近似 RBF|97|不內嵌完整輸出|

移除ID：`dx-cv`, `dx-fit`, `dx-khan`, `dx-map`, `dx-rbf`, `dx-rbfs`, `dx-sep`, `dx-sgd`, `dx-sv`, `qKernFeedback`, `qKernOptions`, `w10-detail-maxmargin-lab`, `w10-detail-multiclass-lab`, `w10rbfChart`, `w10rbfTe`, `w10rbfTr`

### unsupervised_learning

|保留示範|原Lab cell|輸出|
|---|---|---|
|置中、標準化與 PCA 得分|19,21,23,27|不內嵌完整輸出|
|以 SVD 計算主成分|48|不內嵌完整輸出|
|解釋變異比例|39|原PVE短向量|
|以欄平均初始化缺值|60|不內嵌完整輸出|
|設定補值迭代的停止條件|62|不內嵌完整輸出|
|依低秩近似更新缺值|64|不內嵌完整輸出|
|K-means 分群|105|不內嵌完整輸出|
|建立階層式分群|117|不內嵌完整輸出|
|t-SNE 的設定與嵌入|74,75|不內嵌完整輸出|

移除ID：`dx-bi`, `dx-hc`, `dx-km`, `dx-mc`, `dx-pca`, `dx-pve`, `dx-svd`, `dx-tsne`, `dx-var`, `dx-vs`, `w07-detail-biplot-lab`, `w07-detail-scaling-lab`, `w07mcCorr`

## 同步與生命週期

- 刪qDeg（自製多項式CV數字題）、qVote（另造數值投票題）、qKern（自製gamma跑分案例題），不新增替代題。SVM EX回饋移除原Lab跑分敘事，保留原課本問題與概念。
- Ch8題庫移除作者核對式剪枝題、800點自造impurity題及依賴數字/實作比較的OOB題；保留RF/boosting/重要度概念。詞卡同步移除Permutation importance與PHATE，刪重現建議。
- 被移除的poly MSE子圖及RBF測試曲線不再初始化/更新；相關被刪rows的寫入一併移除。RF比較init為安全空函式，資料本身保留。
- GCV勝出曲線/按鈕與callback移除；GAM只保留各成分與必要複雜度，不回報R2/GCV榜。補值仍顯示演算法MSS/迭代狀態，不回報真值相關係數。
- 既有AdaBoost收合停止與SVM完整dual深連結機制保留。
- 刪除空Lab標題與空收合；四頁頁內hash連結靜態檢查均無失效。

## 驗證狀態

- 所有FRAMES hash與本輪前快照相同。
- 保留37個原Lab片段，僅PVE保留1個必要短原輸出；全部34個既有proof單元仍在（合併的kernel-Ridge/one-class proof已裁去前者）。
- 原碼連續片段靜態比對及原 `check_lab_rendered.py` 均通過；全站218張保留卡0失敗，本組37片段和PVE短原輸出均 code=True / output=True。


## 最終驗收與凍結

- 主代理共用build後，四頁正式browser均0問題：`ch7-8-9-12-browser.log`。最後只調整Ch7/SVM的過時側欄字串後，這兩頁補跑也0問題：`ch7-8-9-12-browser-final-text.log`。
- 原strict Lab檢查完整log：`ch7-8-9-12-strict-lab.log`；沒有執行notebook或重做數值實驗。
- `ch7-8-9-12-reader-text-scan.json`：四頁讀者文字的已列報告字串/題外方法命中均為空；四頁 `.ver-note` 均0。程式/內部JS/data-source-note不當成學生報告文案。
- `ch7-8-9-12-reader.json`：四頁初始reading-detail全收合；冷啟動hash `w10-primal-dual` / `w10-soft-dual` 均展開且可見，完整數學內容/各自proof仍在，MathJax錯誤0。
- 已實際檢視四頁defaultview截圖（poly、rf、kernel、manifold）：沒有被刪圖表留下的空面板或顯示報告排行榜；核心公式、主要幾何圖與適當的概念自測仍可讀。截圖只存於本次 `shots/ch7-8-9-12/`。
- 凍結時不再生成HTML；沒有commit/push。刪除區塊留下的原碼空白不做格式化，以免破壞Lab連續片段保真。
