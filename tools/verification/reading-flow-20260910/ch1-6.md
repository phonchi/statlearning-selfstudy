# Ch1–6 閱讀流程整理（2026-09-10）

> 保真結論更正：初輪自有檢查使用 BeautifulSoup 預設解析器，連同舊snapshot一起正規化了Python縮排，因此其「Lab逐字一致」結論不可靠。以本次修復後未修改的原 `tools/check_lab_rendered.py` 結果為準：全245張卡，failures=0。詳見文末。

已完成六章內容組裝與局部生成。共用樣式、`detail`、收合生命週期及書目／詞卡／題庫外框由主代理負責；本組不修改共用框架，沒有 commit 或 push。

## 實作與正文主線

母檔為六支既有 enrich，章專用組裝在 `tools/enrich/reading_flow_ch1_6.py`。它把實際段落、公式、長例、code/output與相關proof整組裝入 `lib.detail`，不是只加一個入口卻留下展開全文。

|章|維持可見的主線|整組收合|
|---|---|---|
|1|資料與任務、監督/非監督、EDA、八種統計量直覺表、五組讀圖及核心題|十個思想與其延伸題、完整資料集/資源表、Lab完整程式及輸出|
|2|Y=f(X)+ε、條件平均、誤差/彈性、維度直覺與一個短例、Bayes與KNN|高維球/球殼/體積/長表/Gamma/Stirling/R計算與對應互動、KNN有效自由度、Lab|
|3|OLS與係數、CI/PI用途和必要假設、R²/F、交互作用與診斷、主要直覺互動與自測|RSS參數曲面/矩陣推導、固定X、bootstrap區間、部分F/FWL/CCPR、中心化、學生化/殘差幾何、完整數值與Lab|
|4|機率/logit/概似目標、LDA/QDA/NB分類規則、門檻與ROC、Poisson均值與變異|Hessian/IRLS/資訊與CLT、機率區間與潛在模型、多類估計/OVR/OVO、Fisher整組與Iris延伸題、QDA完整係數、GLM細節、長數值與Lab|
|5|CV三法、切分原則、折內前處理、bootstrap流程/SE/基本未抽到機率、核心題|PRESS/秩、nested/OOF、各種區間、Jackknife、Permutation、偏差修正、預測誤差與block/conformal、Lab|
|6|subset/stepwise、比較準則和必要條件、one-SE、Ridge/Lasso目標與幾何、折內調參、PCR/PLS與高維限制|Cp風險/離差、Ridge矩陣/SVD、Lasso次梯度/座標下降/MAP、LAR/Group、λ–s KKT、PCA白化/重建/獨立、PLS完整算法、長數值與Lab|

Ch1指定的「同一組資料，親手比較摘要」「中位數的穩健性與相對效率/證明」「資料表形狀、合併鍵與來源」維持完全刪除，沒有在折疊內恢復。八項統計量直覺表保持可見。

Ch2 `qCur` 母檔和生成結果同步改為概念題，不依賴藏起來的89%或Gamma計算，避免四維必定失效、更多資料必然無用等過強推論。`regfunc`銜接亦依指定改為「維度提高後，要同時做到這兩點通常需要更多資料」。

核心條件/定義已提到原QUIZ前。只有歷史思想與Fisher兩個延伸自測跟著相應教學收合並標為「延伸自測」；核心方法自測保留正文。原proof最多位於外層reading-detail內，總深度不超過兩層。

## 互動生命週期與深連結

- `w02-detail-geometry`：包含 `w02curSvg`、`w02curChart`、`w02curP`；`HC.onDetail` open呼叫 `w02curDraw()`。
- `w03-detail-rss-surface`：包含 `w03rssSvg`、`w03rssU`、`w03rssV`；open呼叫 `w03rssRender()`。主圖圖例改為「展開 RSS 計算細節可調整的試探線」。
- `w04-detail-fisher`：包含Fisher教學、Iris例子、`qFisher`及相應proof。
- 本組折入區只有滑桿圖，沒有Player或自訂timer；主要Player互動留正文，故未新增不必要的close/reset，保留原frame。共用Chart resize/stop/hash祖先展開由shared.js提供。

## 驗證與證據

- `ch1-6-before.json`保存整理前六頁Lab全文/expected-output與FRAMES指紋；`check-ch1-6.py`逐一比對，**確認無誤**。詳見`ch1-6-static.log`、`ch1-6-static.json`。
- 六頁新版`browser_check.js`：**0問題**，包含全部reading-detail展開後的controls、Chart、QUIZ、公式與手機檢查。正式log：`ch1-6-browser.log`，job session42177已exit0。
- `focus-ch1-6.cjs`：**18項PASS**，涵蓋1280px/390px預設closed、Ch2和Ch3關閉重開後slider與計算值不變、slider和兩層proof深連結、無runtime error。結果：`ch1-6-focus.log`、`ch1-6-focus.json`，job session54589已exit0。
- 最後Ch2銜接文字修改後只重建該頁、重跑該頁validator與六頁保真檢查；**確認無誤**，見`ch1-6-ch2-bridge.log`。這個純文字修改未改互動與截圖中的curse區塊。
- 未重跑任何lab、數值frames或實驗。

截圖位於 `shots/ch1-6/`。已實際檢視：`intro-eda-reading-1280.png`、`ch2-curse-reading-1280.png`、`ch2-curse-reading-390.png`、`ch2-geometry-open-1280.png`、`ch2-geometry-open-390.png`、`ch3-rss-open-1280.png`、`ch3-rss-open-390.png`。正文與收合入口可讀，展開後圖表及數值均正常；手機SVG內小標字沿用既有620 viewBox仍偏小，旁邊解說與數值表可讀，沒有新溢出或空圖。

所有本組程序已取得明確完成狀態；後續由主代理整合全站驗收。


## 第二次預設閱讀檢查後的補漏

已移除以 `tr > 10` 判斷是否收合的規則，改依段名與內容角色組裝：

- Ch2「蒙地卡羅的重現性」連同固定seed說明與完整模擬虛擬碼收合為 `w02-detail-monte-carlo`；原 `qBV` 仍可見。
- Ch2「本頁模擬跑出來的數字」的兩張結果表與解說一起收合為 `w02-detail-simulation-results`。
- Ch2–6「公式速查」連標題與完整公式表一起收合為 `wNN-detail-formula-reference`，不再留下孤立的展開標題；不以表格行數決定。
- 六章 `.ver-note` 的重現方式與版本清單都收合為 `wNN-detail-reproducibility`。原數字、式子及來源保留。
- 短方法比較表與核心提醒仍可見，沒有重寫主要概念或折起核心自測。

`check-ch1-6.py` 新增語義檢查：上述指定段名均在reading-detail內、所有ver-note均收合。六頁validator、原Lab/output/FRAMES保真與深度檢查全部通過，見 `ch1-6-lookup-static.log`。

只對新增完整模擬/公式收合的Ch2、5、6執行必要browser：0問題，`ch1-6-lookup-browser.log`；session42810已exit0。純文字focus擷取session90878已exit0。已實看 `shots/ch1-6-lookup/ch2-biasvar-reading.png` 及三張 `*-reference-reading.png`，確認預設主線保留，完整結果、公式與版本不再直接展開。


## Python縮排回歸修復與獨立驗證

原全站 `lab-rendered.log` 顯示245張卡中10張 `code=False, output=True`。原因是`reading_flow_ch1_6.fragment`使用BeautifulSoup預設空白處理：span/div內只有空白的文字節點被壓縮，將Python續行與區塊的縮排改掉。初輪自有snapshot與檢查也使用相同解析器，因而漏報，不能再用該normalized snapshot證明code保真。

修復僅在章內容組裝器設定 `preserve_whitespace_tags={'pre','textarea','span','div'}`，然後由原enrich BODIES重新生成六頁；沒有從已受損HTML修補程式碼，沒有修改Lab、frames或原checker。

原失敗 `lab-rendered.log` 保留；新 `lab-rendered-after-whitespace-fix.log` 已明確成功：`Checked 245 full rendered lab cards; failures=0`。自有 `check-ch1-6.py` 現在先呼叫未修改的原checker，原normalized snapshot只保留可用的FRAMES等證據，不再作code基準。其結果見 `ch1-6-canonical-lab.log`、`ch1-6-static-after-whitespace-fix.log`。

必要的Ch3/5/6瀏覽器回歸0問題，`ch1-6-whitespace-browser.log`（session96950，exit0）。另在實際瀏覽器DOM核對前向選擇程式確實保留四空格縮排，`whitespace-focus.cjs`（session29079，exit0）；已實看 `shots/whitespace-fix/forward-selection-code.png`，巢狀迴圈與函式縮排清楚，原保存輸出保持一致。
