# Ch7、8、9、12 閱讀分層

採直覺、核心公式、短例與主要互動作正文。完整推導前置、算例、進階方法、長表與Lab轉為預設收合；沒有刪除課程Lab或重新計算FRAMES。

母檔：enrich_nonlin.py / enrich_trees.py / enrich_svm.py / enrich_unsup.py。本組組裝：reading_flow_ch7_8_9_12.py；SVM完整dual來源仍在svm_completion.py，於內容完整組裝後再收合。

## 逐節實際入口

### beyond_linearity

|節|預設收合入口|保留可見的主要互動|
|---|---|---|
|prologue|比較各種方法的彈性、邊界與調整參數 (`w08-detail-method-table`)|—|
|poly|深入計算：係數、基底與信賴區間 (`w08-detail-poly-calculation`)；Python 實作與完整輸出 (`w08-detail-poly-lab`)；機率的信賴區間：logit 轉換與算例 (`w08-detail-logit-ci`)|w08polySvg,w08polyMse|
|step|階梯函數的完整編碼與係數解讀 (`w08-detail-step-coding`)；Python 實作與完整輸出 (`w08-detail-step-lab`)；延伸：切段後的交互作用 (`w08-detail-step-interaction`)|w08stepSvg|
|basis|操作比較：勾選基底、組合曲線 (`w08-detail-basis-sandbox`)；Python 實作與完整輸出 (`w08-detail-basis-lab`)|—|
|splines|完整計算：樣條基底、連續性與自由度 (`w08-detail-spline-calculation`)；Python 實作與完整輸出 (`w08-detail-splines-lab`)|w08knotSvg|
|natural|Python 實作與完整輸出 (`w08-detail-natural-lab`)；深入基底：自然樣條與 B-spline 的計算 (`w08-detail-natural-basis`)|w08natChart|
|smooth|有效自由度與留一法的完整公式 (`w08-detail-smoothing-df`)；Python 實作與完整輸出 (`w08-detail-smooth-lab`)；深入計算：平滑矩陣、交叉驗證與 IRLS (`w08-detail-smoothing-computation`)|w08lamChart|
|loess|課程 LOWESS、跨距與實作差異 (`w08-detail-lowess-lab-plot`)；Python 實作與完整輸出 (`w08-detail-loess-lab`)；局部加權擬合的算例與多變數延伸 (`w08-detail-local-calculation`)|w08loessSvg|
|gam|GAM 模型規格、Python 實作與完整輸出 (`w08-detail-gam-lab`)；完整演算法：偏殘差與可識別的 backfitting (`w08-detail-gam-backfit`)；分類 GAM 的稀有事件算例 (`w08-detail-gam-classification`)；延伸：薄板樣條、效果圖與部分迴歸 (`w08-detail-gam-thinplate`)|w08gamYear,w08gamAge,w08gamEdu|
|exercises|進一步練習：計算與推理題 (`w08-detail-exercises`)|—|
|reference|來源：完整書目與章節定位 (`w08-detail-sources`)；完整比較表、公式與實測結果 (`w08-detail-reference`)|—|

### tree_based_methods

|節|預設收合入口|保留可見的主要互動|
|---|---|---|
|prologue|比較樹與線性模型的性質 (`w09-detail-tree-compare`)；Python 實作與完整輸出 (`w09-detail-prologue-lab`)|—|
|grow|Python 實作與完整輸出 (`w09-detail-grow-lab`)；深入搜尋：連續切點、類別分割與樹演算法 (`w09-detail-split-search`)|w09growSvg|
|prune|完整剪枝：臨界值、路徑與交叉驗證 (`w09-detail-prune-compute`)；Python 實作與完整輸出 (`w09-detail-prune-lab`)|w09pruneChart|
|classtree|完整算例：錯誤率相同，為何分裂選擇不同？ (`w09-detail-impurity-calculation`)；分類樹的分裂、剪枝與 Python 實作 (`w09-detail-classtree-lab`)|w09impSvg|
|vslinear|無本組新增收合|—|
|why|深入投票：二項機率、獨立條件與算例 (`w09-detail-voting-probability`)；Python 實作與完整輸出 (`w09-detail-why-lab`)|—|
|bagging|Python 實作與完整輸出 (`w09-detail-bagging-lab`)|w09bagSvg|
|rf|深入隨機化：相關性公式、Extra-trees 與抽樣層次 (`w09-detail-extra-trees`)；Python 實作與完整輸出 (`w09-detail-rf-lab`)|w09rfChart|
|boosting|對照樹數、學習率與深度 (`w09-detail-boost-parameters`)；延伸：AdaBoost 如何重新分配權重 (`w09-detail-adaboost-demo`)；Python 實作與完整輸出 (`w09-detail-boosting-lab`)；一般負梯度、AdaBoost 尺度與不同損失 (`w09-detail-gradient-general`)|w09gbSvg|
|modern|套件實作、超參數與實測比較 (`w09-detail-modern-comparison`)；XGBoost 的完整目標、葉值與分裂增益 (`w09-detail-xgboost-math`)；搜尋加速、CatBoost 順序與 DART (`w09-detail-boost-engineering`)|—|
|stacking|如何讀變數重要度與置換重要度 (`w09-detail-importance`)；Stacking 的折外預測與完整實作 (`w09-detail-stacking`)；BART 的完整模型、實作與後驗更新 (`w09-detail-bart`)；自我檢查：折外預測與重要度 (`w09-detail-stacking-check`)|—|
|exercises|進一步練習：計算與推理題 (`w09-detail-exercises`)|—|
|reference|來源：完整書目與章節定位 (`w09-detail-sources`)；完整比較表、公式與實測結果 (`w09-detail-reference`)|—|

### support_vector_machines

|節|預設收合入口|保留可見的主要互動|
|---|---|---|
|prologue|Python 實作與完整輸出 (`w10-detail-prologue-lab`)；計算：點到超平面的距離 (`w10-detail-distance`)|w10hyperSvg|
|maxmargin|完整硬邊界問題：primal、dual、KKT 與算例 (`w10-detail-hard-dual`)；Python 實作與完整輸出 (`w10-detail-maxmargin-lab`)；延伸：從感知器到最大邊界 (`w10-detail-perceptron`)|w10marginSvg|
|soft|完整軟邊界對偶：slack、限制、KKT 與截距 (`w10-detail-soft-dual`)；Python 實作與完整輸出 (`w10-detail-soft-lab`)|w10softSvg,w10softChart|
|hinge|完整目標與尺度：hinge、slack、預算及標籤 (`w10-detail-loss-normalization`)；對照分數的位置與支持向量 (`w10-detail-hinge-cases`)；Python 實作與完整輸出 (`w10-detail-hinge-lab`)|w10lossChart|
|kernel|深入核函數：特徵映射、Gram 矩陣與合法條件 (`w10-detail-kernel-validity`)；Python 實作與完整輸出 (`w10-detail-kernel-lab`)；算例：用 SVM 決策分數畫 ROC (`w10-detail-score-roc`)|w10kernSvg、w10rbfSvg,w10rbfChart|
|multiclass|OVO／OVA 完整比較與 SVC 介面 (`w10-detail-multiclass-table`)；Python 實作與完整輸出 (`w10-detail-multiclass-lab`)|—|
|vslogit|完整對照：SVM、邏輯斯損失與 LDA (`w10-detail-model-comparison`)；Python 實作與完整輸出 (`w10-detail-vslogit-lab`)；延伸：SVR 的管狀損失與完整對偶 (`w10-detail-svr`)；大規模求解、隨機 Fourier 特徵與核近似 (`w10-detail-solvers`)；延伸：核 Ridge 與單類別 SVM (`w10-detail-kernel-extensions`)|—|
|exercises|進一步練習：計算與推理題 (`w10-detail-exercises`)|—|
|reference|來源：完整書目與章節定位 (`w10-detail-sources`)；完整比較表、公式與實測結果 (`w10-detail-reference`)|—|

### unsupervised_learning

|節|預設收合入口|保留可見的主要互動|
|---|---|---|
|prologue|延伸用途：密度估計與異常偵測 (`w07-detail-other-goals`)|—|
|pca|完整 PCA：限制式、矩陣算例與特徵推導 (`w07-detail-pca-advanced`)；Python 實作與完整輸出 (`w07-detail-pca-lab`)|w07spinSvg|
|biplot|Python 實作與完整輸出 (`w07-detail-biplot-lab`)|w07biSvg|
|lowrank|完整低秩計算：SVD、變異分解與不唯一性 (`w07-detail-lowrank-advanced`)；Python 實作與完整輸出 (`w07-detail-lowrank-lab`)|—|
|pve|Python 實作與完整輸出 (`w07-detail-pve-lab`)；深入選維度：驗證目標與資訊準則 (`w07-detail-pca-rank-selection`)|w07screeChart|
|scaling|Python 實作與完整輸出 (`w07-detail-scaling-lab`)；深入計算：共變異數、白化與零特徵值 (`w07-detail-scaling-advanced`)|—|
|completion|Python 實作與完整輸出 (`w07-detail-completion-lab`)；補值驗證、可識別性算例與單調性推導 (`w07-detail-completion-advanced`)|w07mcSvg,w07mcChart|
|kmeans|完整 K-means：平方和、算例、停止條件與證明 (`w07-detail-kmeans-advanced`)；Python 實作與完整輸出 (`w07-detail-kmeans-lab`)|w07kmSvg|
|hclust|查閱四種 linkage 的完整定義 (`w07-detail-linkage-table`)；Python 實作與完整輸出 (`w07-detail-hclust-lab`)；深入階層方法：Ward 算例、分裂式與 MST (`w07-detail-hclust-advanced`)|w07dendroSvg|
|practical|實作、圖表資料來源與完整輸出 (`w07-detail-practical-lab`)；延伸方法概觀與密度分群的完整流程 (`w07-detail-density`)；混合模型：EM、責任值與變分貝氏 (`w07-detail-mixtures`)；譜分群的圖、嵌入與數學條件 (`w07-detail-spectral`)；混合資料、其他分群方法與評估指標 (`w07-detail-mixed-clustering`)|w07practSvg|
|manifold|延伸方法、套件用途與理論保證範圍 (`w07-detail-manifold-extensions`)；Python 實作與完整輸出 (`w07-detail-manifold-lab`)；完整 t-SNE：鄰居機率、參數、梯度與算例 (`w07-detail-tsne-algorithm`)|w07tsneChart|
|exercises|進一步練習：計算與推理題 (`w07-detail-exercises`)|—|
|reference|來源：完整書目與章節定位 (`w07-detail-sources`)；完整比較表、公式與實測結果 (`w07-detail-reference`)|—|

## 保留與依賴

- 保留各章概念quiz；qGrow候選數與qVote投票數值題隨詳細計算收合。qModern位於套件深讀；qStack是進階stacking自測。qDualBounds跟著完整軟邊界單元。
- SVM正文保留分數、hinge核心式、kerneldecision與C/B定義；硬軟primal-dual/KKT分別在完整可展開入口，不拆掉限制式或截距回復。
- 原始Lab/card內容與原有IDs均保留；讀者可由hash展開祖先，生命週期由主代理的shared支援。
- 原正文中的樣條係數計數、PCA大矩陣最佳化、SVM max-M/slack/損失尺度也一起收合；正文加入短的完整解釋，避免只把proof藏起來。
- 公式表與實測表由各頁BODIES reference收合；詞卡/題庫/書目由主代理共同builder處理，本組不改GEN區。

## 生命週期

- 唯一新收合的播放器：w09-detail-adaboost-demo。close callback呼叫w09adaPlayer.stop()，保留目前frame；按鈕同步為「▶ 開始」、aria-pressed=false。不自動重新播放。
- w08loessPlayer、w09growPlayer、w09gbPlayer、w10kernPlayer保留正文；不需要加收合hook。
- w07mcTimer/w07mcGen與w07kmTimer/w07kmGen的補值與K-means主互動保留正文，未被放入reading-detail；因此沒有新增不可見背景運轉情境，也不變更原本演算法。
- 新收合Chart.js：額外LOWESS課程圖、投票機率、進階變數重要度；展開尺寸更新使用共享HC.refreshDetail。

## 驗證

- 結構核對已完成，結果見ch7-8-9-12-structure.log。
- 等主代理統一build完成後，執行四頁browser及AdaBoost關閉/重開定位專項；不將未執行檢查寫成通過。


## 完成驗收

- 四頁正式 `ch7-8-9-12-browser.log`：全部 OK，0 個問題。包含初始收合、展開後互動與 MathJax、手機版、離線與原有元件驗收。
- `ch7-8-9-12-lifecycle.log/json`：AdaBoost 在 frame 2 播放中收合後停止，等待後仍為 frame 2，重開也不自動播放；按鈕同步為「▶ 開始」。
- `#w10-soft-dual` 深連結成功展開 `w10-detail-soft-dual`，完整對偶限制與截距回復可見，沒有 MathJax 錯誤；截圖已實際檢視。
- 四頁 default section 截圖（Ch7 smooth、Ch8 modern、SVM soft、Ch12 practical）均已實際檢視：核心圖與概念題可直接閱讀，進階單元以具體主題列出。完整 reading/mobile 截圖亦由正式驗收保存。
- 四頁 `validate.py --page` 均 0 失敗；Ch7/8/12只有 SIZE 建議警告，SVM無警告。
- 首輪驗收發現原始數學文字中的裸小於號被 HTML 解讀成標籤，留下 `$0`、`$r` 等殘片。本組已在原 BODIES 交給 reader_sources.fragment 前轉義數學文字中的小於號；pre/code受保護，不從已毀損DOM猜回公式。修正後排除code的文字節點檢查三頁均無殘留，50張原始完整Lab卡、全部錨點與FRAMES再次確認保留。
- 首輪失敗log另存 `ch7-8-9-12-browser-before-tex-fix.log`，不混記為通過。沒有重跑Lab、生成新數值資料或執行commit/push。
